(defpackage chun-git-test
  (:use :cl
        :chun-git
        :prove))
(in-package :chun-git-test)

;; NOTE: To run this test file, execute `(asdf:test-system :chun)' in your Lisp.

(plan nil)

(finalize)
